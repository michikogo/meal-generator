import React from "react";

const AddMovie = () => {
  return (
    <div>
      Users can create movies, descriptions, genre and have initial rate I am
      excited to design, build and support client systems. I believe that for
      clients to keep comming back to the website it should have good UI/UX.
    </div>
  );
};

export default AddMovie;
